# Server
PORT=3000
NODE_ENV=development

# local | production
MODE=local

# PostgreSQL
LOCAL_DATABASE_URL=postgres://postgres:Qwerty%40123@localhost:5432/defiBot
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=astra

# JWT
JWT_SECRET=your-secret-key

# Email (SMTP)
# Public backend URL used inside verification email link
API_PUBLIC_URL=http://localhost:3000
# Frontend login page for the “Log in to ASTRA” button
FRONTEND_LOGIN_URL=https://astra-frontend-lemon.vercel.app/login

# Backward-compatible (older configs). Prefer API_PUBLIC_URL / FRONTEND_LOGIN_URL.
APP_URL=http://localhost:3000
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-user
SMTP_PASS=your-pass
SMTP_FROM="Astra <no-reply@example.com>"
